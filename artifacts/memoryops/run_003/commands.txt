# Provider discovery only — no ingest executed
printenv | sed 's/=.*/=REDACTED/' | grep -Ei 'OPENAI|ANTHROPIC|XAI|GEMINI|GROQ|VOYAGE|OPENROUTER' || true
python -c 'import os; print({k:(bool(os.getenv(k))) for k in ["OPENAI_API_KEY","ANTHROPIC_API_KEY"]})'
