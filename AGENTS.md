# 🤖 Graphiti_fractal_lab — agent contract

Mandatory entry for AI coding agents and automated auditors.

## Start here

1. [`docs/ai/README.md`](docs/ai/README.md) — machine-first identity, reading order, invariants.
2. [`RESEARCH_STATUS.md`](RESEARCH_STATUS.md) — what is smoke-proven vs NOT VALIDATED; mirror honesty.
3. Exact-head `src/fractal_lab/` + lab smoke tests + `pytest` when claiming lab results.
4. Mirrored Fractal tree (`core/`, `api/`, …) is for **experimentation** — do not claim it runs without Docker/secrets.

Human narrative (`README.md` LAB NOTICE + Fractal body, `SYSTEM_OVERVIEW.md`) explains; it is **not** live proof.

## Hard rules

- This repo is a **RESEARCH lab** with a **full mirrored Fractal tree**, not production Fractal Memory authority.
- Do **not** claim Neo4j / Graphiti / `graphiti_core` parity for the Ladybug path.
- Do **not** treat Ladybug lab graphs as durable Fractal authority.
- Do **not** modify or push to `velantrian/Graphiti_fractal`.
- Stubs (`kuzu` / `postgres` / `duckdb`) remain **NOT VALIDATED** until evidence exists.
- Mirrored `docker-compose.yml` / Neo4j path still requires Docker + secrets — **not claimed runnable in this lab box without them**.
- `file exists ≠ tested ≠ active Fractal path ≠ production/migration authorized`.

## Status language

Use: ✅ lab smoke · 🔬 research/stub · ❌ not claimed / not authorized.

Re-run `pytest` (lab smoke subset or full suite with gates) before stating pass counts.
